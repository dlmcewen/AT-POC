{
  "name": "CypressTest",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run1": "node runCypressTests.js",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --record --key RUN_KEY",
    "cypress:runX": "cypress run --record --key RUN_KEY --reporter cypress-testrail-reporter",
    "cypress:verify": "cypress verify"
  },
  "license": "ISC",
  "devDependencies": {
    "cypress": "^3.1.0",
    "cypress-testrail-reporter": "^1.1.0"
  }
}
